import { PrefixPostfixPipe } from './prefix-postfix.pipe';

describe('PrefixPostfixPipe', () => {
  it('create an instance', () => {
    const pipe = new PrefixPostfixPipe();
    expect(pipe).toBeTruthy();
  });
});
